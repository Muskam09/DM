# Технічне Завдання (ТЗ): AI-асистент "D&T Hotel"

## 1. Опис проекту
Проект являє собою AI-асистента для готелю "D&T Hotel", інтегрованого з омніканальною платформою Chatwoot. Бот автоматизує первинну комунікацію з клієнтами, презентує номерний фонд, перевіряє наявність вільних місць (через парсинг зовнішньої "Шахівниці") та проводить розрахунок вартості проживання за чітко визначеними бізнес-правилами.

## 2. Технічний стек та Архітектура
* **Мова та Фреймворк:** Python 3.11+, FastAPI, Uvicorn.
* **AI Провайдер:** Google GenAI SDK (модель `gemini-2.5-flash-lite`).
* **Скрапінг:** Playwright (асинхронний збір даних наявності номерів).
* **Інтеграція:** Webhook-сервер, який приймає події `message_created` від Chatwoot API та відправляє відповіді через POST-запити.
* **Інфраструктура:** Docker Compose (сервіси `bot-brain`, `rails`, `postgres`, `redis`, `sidekiq`). Зв'язок між Chatwoot та ботом відбувається через внутрішню Docker-мережу.

## 3. Структура проекту
Проект базується на строгому розділенні логіки та контенту:
* `bot_server.py` — ядро програми. Містить FastAPI сервери, логіку кешування, `intent_prompt` (для визначення потреби запуску скрапера), та основний `sys_prompt` (State Machine).
* `templates.py` — база знань. Містить усі текстові шаблони (FAQ, презентації, описи номерів). Бот не генерує текст самостійно, а лише компонує ці шаблони.
* `hotel_scraper.py` — модуль для парсингу "Шахівниці" та формування JSON з наявністю номерів.
* `pricing.json` — конфігураційний файл з актуальними цінами.

## 4. Патерни та Алгоритми (КРИТИЧНО ДЛЯ CLAUDE)
1. **Intent Analyzer (Пре-фільтр):** Перед тим як формувати відповідь, викликається легкий LLM-запит, який визначає, чи потрібен доступ до бази даних готелю. Скрапер запускається **тільки** якщо клієнт називає усі чинники, які впливають на вартість (Тип номеру, кількість дорослих, кількість та вік дітей, дати бронювання, кількість ночей), АБО змінює дати для вже обраного номеру, АБО погоджується на пошук вільних дат.
2. **Hidden State (Теги мислення):** Основна модель зобов'язана виконувати всі розрахунки, збір контексту та логічні кроки виключно всередині тегів `<THINK>...</THINK>`.
3. **Regex Extraction:** У `bot_server.py` реалізовано регулярний вираз `re.search(r"<REPLY>(.*?)</REPLY>")`, який гарантує, що клієнт у Chatwoot отримає **лише** фінальний текст із тегу `<REPLY>`, а не машинну логіку.
4. **Спліттинг повідомлень:** Реалізовано імітацію "людського" набору тексту. Якщо LLM повертає маркер `[SPLIT]`, `bot_server.py` розрізає повідомлення і відправляє їх частинами із затримкою `asyncio.sleep(1.5)`.
5. **Фільтрація JSON (Blacklist):** На рівні Python реалізовано масив `IGNORE_CATEGORIES = ["Колиба", "Басейн", "Overbooking"]`. Ці сутності примусово видаляються з результатів скрапера, щоб бот ніколи не пропонував їх як кімнати.

## 5. Бізнес-логіка та Правила ціноутворення
* **Математика ночей:** Рахується строго як `Дата виїзду мінус Дата заїзду`. День виїзду не оплачується і не перевіряється в базі наявності.
* **Тарифікація (Календар 2026):** Будні дні — Нд, Пн, Вт, Ср, Чт. Вихідні дні (потребують тарифу "вихідні") — П'ятниця та Субота.
* **Діти та Додаткові місця (УВАГА! ТАРИФНА ТАБЛИЦЯ — оновлено власником 2026-06-23):** Базова `вартість_кімнати` покриває до базової місткості номеру (2 особи, для УСІХ типів, включно з Напівлюксом). За кожного гостя ПОНАД базову місткість додається доплата **за кожну ніч**, за віковою шкалою (інтервали напіввідкриті — межі однозначні):
  * **Дитина 0–5 років (до 6):** проживає **безкоштовно** (спить з батьками, доплата 0 грн, окремого платного місця не займає).
  * **Дитина 6–11 років:** додається `дитяче_місце` (це вже 50% тариф на додаткове ліжко). Рівно 6 років → `дитяче_місце`.
  * **Гість від 12 років АБО додатковий дорослий:** додається `додаткове_місце` (повний тариф). Рівно 12 років → `додаткове_місце`.
  * Базову місткість заповнюємо найдорожчими гостями (дорослі / діти 12+), тому "зайвими" (платними) стають найдешевші гості.
  * Якщо платний гість лише один — замість `вартість_кімнати` застосовується `одномісне_поселення` (де він заданий).
  * Приклади: для 3 дорослих — `(вартість_кімнати + додаткове_місце) * кількість ночей`; для 2 дорослих + 1 дитина 8 років — `(вартість_кімнати + дитяче_місце) * кількість ночей`.
  * Тариф (будні/вихідні) визначається ОКРЕМО для кожної ночі — підсумовуй по ночах.
* **Long-term Memory:** Бот зобов'язаний збирати дані ("Дати", "Кількість гостей", "Ночі") з усієї історії діалогу, навіть якщо клієнт надавав їх по одному слову в різних повідомленнях. Бот не має перепитувати вже відому інформацію.

## 6. User Cases & Expected Behavior (Еталонна поведінка бота)
Заборонено ламати ці сценарії під час рефакторингу або додавання нових фіч.

### Кейс 1: Перший контакт (Немає вхідних даних)
* **Вхідне повідомлення (Клієнт):** "Доброго дня, хочу забронювати номер!"
* **Внутрішній стан (Intent):** Скрапер **НЕ** запускається.
* **Очікувана дія бота:** Фіксує відсутність даних. Додає Привітання + `[SPLIT]`. Видає шаблон `QUESTION_ALL_MISSING`.

### Кейс 2: Надання дат без вибору номеру
* **Вхідне повідомлення (Клієнт):** "28-29 Червня, для двох дорослих"
* **Внутрішній стан (Intent):** Скрапер **НЕ** запускається.
* **Очікувана дія бота:** Зберігає дати. Визначає місяць (Червень). Видає загальний шаблон `PRICE_JUNE`.

### Кейс 3: Успішний вибір номеру та розрахунок
* **Вхідне повідомлення (Клієнт):** "Давайте номер типу Стандарт +"
* **Внутрішній стан (Intent):** Скрапер **ЗАПУСКАЄТЬСЯ**. Клієнту відправляється "Секундочку...".
* **Очікувана дія бота:** Читає контекст (28-29 червня, 2 дорослих). Перевіряє JSON (вільно). Визначає, що це 1 будня ніч. Рахує ціну. Видає заповнений шаблон `PRICE_CALLCULATION`.

### Кейс 4: Частковий Overbooking (Обраний номер зайнятий, є інші)
* **Вхідне повідомлення (Клієнт):** "Стандарт +"
* **Статус Шахівниці:** "Стандарт+" = 0. "Напівлюкс" = 2.
* **Очікувана дія бота:** Бачить `0`. Сканує масив на наявність інших номерів. Знаходить "Напівлюкс". Використовує шаблон `ROOM_BOOKED`, підставляючи **тільки** реально вільні номери.

### Кейс 5: Повний Sold Out та пошук найближчих дат
* **Вхідне повідомлення (Клієнт):** "Стандарт +"
* **Статус Шахівниці:** Всі категорії = 0.
* **Очікувана дія бота:** Розуміє, що вільних номерів немає взагалі. Видає фразу: *"На жаль, на ваші дати всі номери повністю заброньовані... Підкажіть, чи підшукати вам варіанти?"*.
* **Продовження (Клієнт):** "Так".
* **Очікувана дія бота:** Скрапер запускається. Бот сканує Шахівницю вперед від дат клієнта, знаходить першу вільну дату для "Стандарт+". Видає шаблон `NEAREST_DATES`.

### Кейс 6: Збір даних "по краплях"
* **Історія діалогу:** "5 липня" -> "дитині 7 років" -> "2 дорослих" -> "1 ніч".
* **Очікувана дія бота:** Консолідує всі дані в `<THINK>`. Не перепитує. Коли всі дані зібрано, переходить до видачі прайсу або розрахунку.

### Кейс 7: Сувора математика (Додаткові місця)
* **Вхідні дані (Клієнт):** "6-8 липня, 2 дорослих і дитина 8 років, номер Стандарт"
* **Очікувана дія бота:** 1. Визначає 6-8 липня як 2 будні ночі (6 та 7 липня 2026 — Пн і Вт).
    2. Бачить, що гостей 3 (2 дорослих заповнюють базову місткість 2 + 1 дитина 8 років — зайва, платна).
    3. Дитина 8 років потрапляє у діапазон 7–12 → доплата `дитяче_місце` (300 грн/ніч).
    4. Рахує ціну за формулою: `(вартість_кімнати + дитяче_місце) * 2 ночі` = `(2200 + 300) * 2 = 5000 грн`.
    5. Заповнює і видає шаблон `PRICE_CALLCULATION`.
* **Примітка:** діти 0–6 років були б безкоштовні (доплата 0); гість старше 12 років або 3-й дорослий рахувався б за `додаткове_місце`. Див. §5.

### Кейс 8: Зміна умов на льоту (Перерахунок)
* **Вхідне повідомлення (Клієнт):** "А яка вартість буде на 2 ночі?" АБО "А якщо 10-15 серпня?"
* **Внутрішній стан (Intent):** Скрапер **ОБОВ'ЯЗКОВО ЗАПУСКАЄТЬСЯ**.
* **Очікувана дія бота:** Оновлює контекст дат/ночей. Наново перевіряє доступність номеру в Шахівниці на нові дати. Робить новий розрахунок.

## 7. Розширені User Cases та глобальні правила (бізнес-рішення власника, 2026-06-16)

Сформовано після аналізу 1091 реального діалогу з Instagram Direct (див. `new_user_cases_report.md`).

### Глобальні правила (діють НАД усіма кейсами)
* **МОВА:** Бот ЗАВЖДИ відповідає **виключно українською**, навіть якщо клієнт пише російською чи іншою мовою (жорстка інструкція в `sys_prompt`, Правило 0).
* **СПАМ (B2B):** Рекламні/комерційні повідомлення (стікери, чат-боти, таргетологи, SMM, "пробний тариф" тощо) бот **повністю ігнорує — НЕ надсилає жодної відповіді** у Chatwoot. Детекція: `bot_logic.is_spam()` (рання зупинка в `process_incoming_message`).
* **НОМЕР ТЕЛЕФОНУ:** Якщо клієнт залишає номер телефону, бот відповідає `PHONE_RECEIVED` ("…передав ваш контакт менеджеру…") і зупиняє діалог. Детекція: `bot_logic.contains_phone_number()`.
* **МІСЯЦІ З ЦІНАМИ:** Ціни визначені ТІЛЬКИ на Червень/Липень/Серпень (`pricing_engine.PRICED_MONTHS`). Для інших місяців — кейс «Міжсезоння».

### Кейс 9: Великі групи (20+), табори, тури, заходи/банкети
* **Вхід:** "група 25 чоловік", "10 дорослих і 12 дітей", "спортивні збори", "весілля", "корпоратив", "захід".
* **Поріг:** 20+ гостей (`bot_logic.LARGE_GROUP_MIN`), за текстом (`looks_like_large_group`)
  АБО за сумарною кількістю гостей у слотах (`slots_total_guests`, дорослі+діти по всіх номерах).
* **Дія:** НЕ бронювати. Видати `LARGE_GROUPS_EVENTS` (перенаправлення до співвласника: 0673445220, Ярослав).

### Кейс 10: Бронювання кількох номерів (мульти-бронювання, до 8)
* **Вхід:** "хочемо 2 номери", "дві сім'ї".
* **Дія:** У `<THINK>` рахувати КОЖЕН номер ОКРЕМО (Крок 1: Номер 1…, Крок 2: Номер 2…), потім ПІДСУМУВАТИ і вивести розбивку + загальну суму. Детермінований еталон: `pricing_engine.quote_multiple()`.

### Кейс 11: Міжсезоння (вересень–травень)
* **Дія:** Видати `OFF_SEASON` ("…вартість номерів ще не визначена.").

### Кейс 12: Нечіткі дати ("на літо")
* **Дія:** Видати `FUZZY_DATES` ("Який саме місяць та дати вас цікавлять?").

### Кейс 13: Кінець воронки ("я подумаю")
* **Дія:** Видати `THINKING_ABOUT_IT` і **НЕ ставити жодних додаткових питань**.

### Кейс 14: Дати не підходять і все заброньовано
* **Дія:** Ввічливо завершити діалог: `POLITE_CLOSE`.

### Нові FAQ-категорії (шаблони в `templates.py`)
* `SAUNA_VATS` — чани/сауна (на стадії будівництва, партнери поруч).
* `FOOD_PRICES` — ціни на харчування (сніданок 350 / обід 450 / вечеря 450; комплекси по місяцях).
* `TRANSFER_PARKING` — паркінг (входить у вартість) і трансфер з Ворохти (~1000 грн).
* `ROOM_AMENITIES` — WiFi, енергонезалежність (сонячні панелі), мангал (дрова ~250 грн окремо).
* `SMOKING` — куріння в номерах заборонено; можна на вулиці та на балконі Стандарт +.
* `PETS` — оновлено (посилання на правила в Instagram Highlights).

## 8. Архітектура v2 — детермінований рушій (LLM лише екстракція)

Після виявлення помилки LLM у даті (5 липня — «п'ятниця») математику ціни винесено
з LLM. Тепер LLM **лише витягує структуровані дані** (slots), а всю логіку виконує Python.

* **Екстракція (LLM):** `bot_server.EXTRACTION_PROMPT` повертає JSON
  `{topic, rooms[], faq_template}`. Дати/ночі/гості/номер консолідуються з УСІЄЇ
  історії (обробка «по краплях»). LLM НЕ рахує ціни, дні тижня чи суми і НЕ пише
  текст для клієнта.
* **Детермінований рушій (`dialogue_engine.py` + `pricing_engine.py`):**
  - `plan(slots)` → питання про брак даних / OFF_SEASON / місячний прайс / `quote`.
  - `finalize_quote(rooms, availability)` → **гейтинг наявності ПЕРЕД ціною**
    (будь-який зайнятий номер → `POLITE_CLOSE`, без ціни) → розрахунок через
    `pricing_engine` → **сувора форма** виводу.
* **Сувора форма ціни:** `Вартість номеру типу {тип}, для {гості}, на {ночі}
  ({дати}), буде вартувати - {сума} грн`. Для кількох номерів — окремий рядок на
  кожен + `Загальна вартість: {сума} грн`.
* **Детерміновані запобіжники (не на розсуд LLM):** спам → тиша
  (`is_spam`), телефон → передача менеджеру (`contains_phone_number`), велика
  група 20+/захід → перенаправлення (`looks_like_large_group`/`slots_total_guests`), міжсезоння →
  `OFF_SEASON` (`pricing_engine.PRICED_MONTHS`).
* **Мова:** відповіді — це шаблони / Python-текст, тому завжди українською без
  дрейфу.
* **Покриття тестами:** `pricing_engine`, `dialogue_engine` та потік
  `process_incoming_message` (з мок-екстракцією) — 100+ тестів зелені у контейнері.
  Стрес-тест 10 хаотичних реальних діалогів з Instagram пройдено (без галюцинацій
  цін, порушень форми чи помилок наявності).

## 9. Поточний стан системи (повний знімок, оновлюється щоразу)

### 9.1 Порядок обробки вхідного повідомлення (`process_incoming_message`)
1. **MUTE-перемикач:** запит міток конверсації (`get_conversation_labels`). Якщо є
   мітка **`Замовлено`** (або будь-яка з `bot_logic.MUTE_LABELS`) — людина-адмін
   перехопила діалог, бот **повністю мовчить** (без LLM, без відповіді).
2. **Спам** (`is_spam`) → тиша.
3. **Оплата** (`is_payment_intent`: вкладення-скрін АБО ключові слова оплати) →
   відправити `PAYMENT_RECEIVED_HANDOFF`, додати мітку `Замовлено`, замовкнути.
   **Бот НІКОЛИ не підтверджує бронь автоматично** (не може відрізнити реальний
   переказ від фейкового скріна).
4. **Телефон** (`contains_phone_number`) → `PHONE_RECEIVED`, передати менеджеру.
5. **LLM-екстракція** (єдиний виклик LLM) → slots JSON.
6. **Детермінований override великої групи** (`looks_like_large_group` + `slots_total_guests` ≥ 20, або захід).
7. **Маршрутизація** (`route_simple_topic` → інакше `dialogue_engine.plan`).
8. **Котирування** (`action=="quote"`): спершу привітання (якщо перший хід), потім
   `"Секундочку…"`, потім скрапінг, потім `finalize_quote`.

### 9.2 Платіжний хендоф і тегування (Chatwoot Labels)
* Шаблон `PAYMENT_RECEIVED_HANDOFF` просить ПІБ + телефон + джерело (Instagram/…).
* `add_conversation_label` робить `POST /conversations/{id}/labels` (зберігаючи
  наявні мітки через об'єднання). Мітка `bot_logic.ORDER_LABEL = "Замовлено"`.
* Вебхук тепер обробляє повідомлення **лише зі скріном** (без тексту).

### 9.3 Гейтинг наявності (Кейси 4 & 5) — детерміновано
У `finalize_quote`, якщо запитаний (обраний) номер `sold_out` на потрібні ночі:
* є інші вільні категорії → `ROOM_BOOKED` (підставляються лише дійсно вільні);
* нічого вільного (повний sold-out) → форвард-скан ЦЬОГО номеру до 90 днів
  (`find_nearest_window`): знайдено вікно → `SOLD_OUT_FOUND_NEAREST`; нічого у видимому
  календарі → `NEAREST_NONE`.
* Якщо точні дати + гості, але номер НЕ обрано (`finalize_quote_all`) і все зайнято →
  `SOLD_OUT_NEAREST` ("На жаль, всі номери, на вказані вами дати - заброньовані. Підкажіть,
  чи підшукати вам найближчі вільні дати? 🗓️").
* `POLITE_CLOSE` більше **не** використовується для sold-out (лише для `reject_dates`).
* Дата **поза вікном** "Шахівниці" = `unknown` (не sold_out) → котирування проходить.
* **Інфініт-скан (Fix 2):** пошук дат **НІКОЛИ** не передається менеджеру. `NEAREST_NONE`
  не згадує менеджера і не просить телефон — лишає діалог відкритим для інших дат.
  Єдина передача менеджеру — повністю невпізнаний намір (`topic=unknown` →
  `MANAGER_HANDOFF` + мітка `bot_logic.INSTAGRAM_LABEL`).

### 9.4 Консолідація "по краплях" (drip)
* Уся історія діалогу передається екстрактору; дати/гості/номер збираються з УСІХ
  повідомлень. `topic`/`faq_template` — за ОСТАННІМ питанням (виняток: велика група).
* "двоє дорослих" => `adults=2, children_ages=[]` (без дітей) — НЕ перепитуємо про дітей.
* Якщо названо лише місяць (без днів) + гості → загальний `PRICE_*`; місяць поза
  сезоном → `OFF_SEASON`; гості відомі → ніколи не падаємо у `QUESTION_ALL_MISSING`.

### 9.5 УБД (ветеранська) знижка — детермінована математика
* `pricing_engine.apply_military_discount(total)` = `round(total * 0.8)` (−20%).
* Екстрактор ставить `ubd=true` для відповідного номеру, якщо клієнт згадує УБД /
  військового / ветерана ("знижка по УБД на один номер" → лише один номер).
* `finalize_quote` застосовує −20% до позначеного номеру, виводить **остаточну
  знижену суму** + рядок "(з урахуванням знижки УБД -20%)" і додає текст `MILITARY`.

### 9.6 Нові FAQ-шаблони
* `HOW_TO_GET_THERE` — як добратися (потяг до Ворохти → 15 км → автобус/таксі/трансфер).

### 9.7 UX-маршрутизація (динамічні слоти, FAQ-пріоритет, нечіткі/точні дати)
* **Динамічні питання (без спаму):** `plan()` питає ЛИШЕ те, чого бракує —
  гості є/дат немає → `ASK_DATES_ONLY`; дати є/гостей немає → `ASK_GUESTS_ONLY`.
  Монолітний `QUESTION_ALL_MISSING` — тільки на першому контакті (порожні слоти).
* **FAQ-пріоритет (детерміновано):** `bot_logic.faq_override` визначає очевидний
  FAQ (локація/тварини/харчування/трансфер/сауна/куріння/басейн) у поточному
  повідомленні і відповідає ОДРАЗУ, перебиваючи збір слотів (напр. "собачка" →
  PETS). Якщо бронювання неповне — додається м'який нудж `FAQ_DATE_NUDGE`.
  **Басейн → два шаблони:** `_pool_template` розрізняє `GUEST_POOL` (ціна/купання/
  відвідування БЕЗ проживання — "покупатись у басейні", "ціна басейну", "на день")
  від `POOL` (басейн як зручність — "чи є басейн", "з підігрівом?", графік/розмір).
* **Нечіткі дати:** екстрактор ставить `rooms[].fuzzy_date` ("початок серпня").
  Нечіткий несезонний місяць → `OFF_SEASON`. Якщо гостей ще немає — `ACKNOWLEDGE_FUZZY`
  (ехо періоду). Якщо гості Є — **проактивний скан** (див. §9.8 Fix 1), а не прохання дат.
* **Точні дати → календар:** точний діапазон + гості = ЗАВЖДИ розрахунок (не
  загальний місячний прайс). З обраним номером → `finalize_quote`; без номеру →
  `finalize_quote_all` (ціна на КОЖЕН доступний тип на ці дати; sold-out пропускає,
  нічого вільного → `SOLD_OUT_NEAREST`). Місячні шаблони `PRICE_*` більше не використовуються.

### 9.8 Проактивний скан, інфініт-пошук, анти-амнезія FAQ (2026-06-18)

П'ять строгих правил UX після фінального QA живої симуляції:

1. **Проактивний скан (kill the fuzzy loop) — Fix 1.** Якщо відомі **гості + нечіткий
   період** (`fuzzy_date`), `plan()` повертає `explore` і `propose_windows()` ОДРАЗУ
   сканує календар **усередині названого періоду** (`fuzzy_period_range` мапить
   "початок/середина/кінець/перша-друга половина/після N" → діапазон днів) і пропонує
   реальні вільні віконця (`PROPOSE_WINDOWS`: "Я перевірив календар на цей період
   ({fuzzy_date}) — маємо вільні віконця: {found_dates}…"). Невідомі ночі → блоки за
   замовчуванням 2 ночі. Бот **не** перепитує точні дати, коли може сканувати. (Вік
   дитини, якщо невідомий, питаємо окремо — без дат.) Див. також §9.9 (поведінка, коли
   період виходить за межі видимого календаря).
2. **Інфініт-скан (kill the manager handoff) — Fix 2.** `find_nearest_window` сканує до
   90 днів (наскільки сягає "Шахівниця"). Пошук дат **ніколи** не передається менеджеру;
   `NEAREST_NONE` лишає діалог відкритим. Передача менеджеру — лише `topic=unknown`
   (повністю невпізнаний намір) + мітка `Instagram`.
3. **Анти-амнезія FAQ — Fix 3.** Відповідь на FAQ **не стирає** `booking_slots`.
   Екстрактор зобов'язаний заповнювати `rooms[]` усіма відомими даними навіть на FAQ-ходах;
   `dialogue_engine.faq_followup()` після FAQ питає ЛИШЕ те, чого бракує (ніколи
   `QUESTION_ALL_MISSING`, ніколи вже надане), а при повних даних пропонує продовжити
   (`FAQ_CONTINUE_NUDGE`). Хелпер `bot_logic.has_booking_context()`.
4. **Відносні дні тижня → точні дати — Fix 4.** Екстрактор агресивно резолвить
   "п'ятниця / виїзд у неділю / із суботи на неділю" у точні `YYYY-MM-DD`, якщо є орієнтир
   (місяць, діапазон чисел, "після N", %%TODAY%%). Приклад: "п'ятниця, виїзд у неділю
   (після 23-27 червня)" → 2026-06-26 .. 2026-06-28.
5. **Текст sold-out.** `SOLD_OUT_NEAREST` = "На жаль, всі номери, на вказані вами дати -
   заброньовані. Підкажіть, чи підшукати вам найближчі вільні дати? 🗓️".

### 9.9 Поріг великої групи 20+, пам'ять слотів, межа календаря (2026-06-19)

Після автономної живої симуляції 9 персон у Chatwoot:

* **Велика група = 20+ (а не 40+).** `bot_logic.LARGE_GROUP_MIN = 20`. Спрацьовує за
  текстом (`looks_like_large_group`) АБО за сумарною кількістю гостей у слотах
  (`slots_total_guests` ≥ 20, дорослі+діти по всіх номерах) — щоб ловити випадки на
  кшталт "10 дорослих і 12 дітей", де текстовий regex не бачить 20.
* **Пам'ять слотів (детермінована, проти дрейфу екстрактора).** Малий LLM-екстрактор
  іноді ГУБИТЬ слот, який клієнт уже назвав (особливо коли наступне повідомлення —
  FAQ чи репліка не по темі), через що бот перепитував. `bot_server._slot_memory`
  пам'ятає слоти бронювання per-conversation; `bot_logic.merge_room()` дозаповнює
  будь-яке порожнє поле свіжої екстракції із пам'яті (нові непорожні значення завжди
  виграють; дати успадковуються лише коли свіжий хід узагалі не згадує дат). Це
  гарантує відсутність амнезії на рівні Python, а не довіри до LLM.
* **Період поза межами видимого календаря.** "Шахівниця" показує ~6-7 тижнів уперед.
  Якщо нечіткий період ВИХОДИТЬ за цей горизонт і у видимій частині нічого вільного
  (`propose_windows`: `period[1] > vis_max`) — бот НЕ пропонує дати з іншого місяця, а
  ввічливо просить точні дати (`ACKNOWLEDGE_FUZZY`); точні позавіконні дати все одно
  котируються через шлях `unknown`.
* **Симулятор.** `simulate_live_chat.py` тепер ще й читає транскрипти
  (`fetch_transcript`/`print_transcript`) і має режим `--read <id…>` для перегляду без
  повторної ін'єкції. Жива валідація: 9 персон у Chatwoot (account 2), розмови ~#113–#132.

### 9.10 Асинхронність + мульти-кімнати + контекстне «Так» (2026-06-21)

Рев'ю race-condition'ів + 4 бізнес-рішення власника.

**Асинхронні правки (5 архітектурних багів):**
* **Скрейп не скасовується FAQ (Bug 1).** Superseded-хід (зокрема перерваний скрейпом)
  ГЛУШИТЬСЯ (щоб корекція дат не давала застарілого/подвійного котирування), але скрейп
  уже наповнив `AVAILABILITY_CACHE`. Хід-FAQ (`is_faq_reply`) бере наявність із кешу
  (`peek_cached_availability`, БЕЗ нового скрейпу) і доставляє **FAQ-відповідь + результат
  бронювання** (`build_booking_reply`), прибравши `FAQ_CONTINUE_NUDGE`.
* **Фільтр-наповнювач (Bug 2) — детерміновано через зміну слотів.** Сигнал «немає нового» —
  `slots_changed = remember_rooms(merged) != prev_mem` (а НЕ «порожня екстракція», бо
  екстрактор за анти-амнезією перевиставляє слоти). Без зміни бронювання — нового пошуку
  не запускаємо.
* **Bug 3:** нечіткий період + бракує віку дитини → `ACKNOWLEDGE_FUZZY_AGE`. **Bug 4:**
  оновлено `SOLD_OUT_NEAREST`. **Bug 5:** правило діапазону днів тижня у prompt екстрактора.
* **Ідемпотентне привітання** (`bot_server._greeted`) — одне привітання навіть при
  read-after-write lag Chatwoot; помилка скрейпу — за гейтом supersede.

**Бізнес-рішення власника:**
1. **Ціна без точних дат (Decision 1).** Якщо клієнт просить ціну, але дат ще нема —
   НЕ мовчимо: `templates.PRICE_NEED_DETAILS` + повторно показуємо вільні віконця з кешу.
   Якщо точні дати ВЖЕ відомі — просто перекотируємо з кешу.
2. **Контекстне «Так»/«Давайте» (Decision 2).** `bot_logic.is_bare_confirmation` +
   контекст останнього повідомлення бота: після котирування (`is_quote_message`) →
   `booking_confirm` → `BOOK_ROOM` (оплата); після пропозиції вікон
   (`is_window_offer_message`, є `_pending_window`) → застосовуємо ПЕРШЕ віконце
   (`dialogue_engine.first_offered_window`) і котируємо.
3. **Мульти-кімнатна пам'ять (Decision 3).** `_slot_memory[conv]` — це СПИСОК кімнат;
   `bot_logic.merge_rooms`/`remember_rooms` зливають по індексу і ЗБЕРІГАЮТЬ кімнати, які
   поточний хід не згадав (бот більше не «забуває все»).
4. **FAQ завжди штовхає до продажу (Decision 4).** Нудж/результат бронювання додається до
   кожної FAQ-відповіді (анти-спам гасить дублі).

**Тести:** 182 зелені (Layer A + B). Запуск: контейнер `bot-brain` або хост-venv із
залежностями `bot_server` (мережа мокнута).

### 9.11 FAQ не перехоплює проактивний скан + GUEST_POOL (2026-06-23)

Жива QA в Instagram виявила два FAQ-регреси:

1. **FAQ перехоплював скан (Dialogue 1).** Якщо в одному повідомленні є дієвий намір
   бронювання + FAQ ("липень, 4 особи, чи є трансфер?"), бот відповідав на FAQ, але
   замість запуску `explore`/`quote_all`/`quote` додавав загальний `FAQ_CONTINUE_NUDGE`;
   на "Так" перепитував деталі (скан так і не відбувся). **Фікс:** комбінована логіка в
   `bot_server` тепер для FAQ із дієвою дією (`plan()` → `_SCRAPE_ACTIONS`) **виконує
   скан**: відповідає на FAQ, бере наявність із теплого кешу (без подвійного скрейпу)
   або скрейпить, і другим повідомленням віддає РЕАЛЬНИЙ результат (вільні віконця /
   варіанти номерів / котирування). `FAQ_CONTINUE_NUDGE` лишається ЛИШЕ коли дія —
   *питання* (бракує даних). Уточнює §9.10 Bug 1 (тоді брали лише кеш): тепер за потреби
   робимо свіжий скрейп. Уточнює правило «FAQ ніколи не скрейпить» (CLAUDE.md §4.1):
   FAQ із дієвим наміром скрейпить.
2. **GUEST_POOL vs POOL (Dialogue 2).** "яка ціна покупались у басейні" екстрактор
   класифікував як `POOL` замість `GUEST_POOL`. Причому детермінований `faq_override`
   ПРИМУСОВО ставив `POOL` для будь-якого "басейн", перекриваючи навіть LLM. **Фікс:**
   `bot_logic._pool_template` розрізняє за маркерами (`_GUEST_POOL_MARKERS` —
   купання/відвідування без проживання; `_POOL_PRICE_MARKERS` — ціна/вартість, окрім
   "входить у вартість"); `EXTRACTION_PROMPT` отримав явне правило GUEST_POOL vs POOL.

## 10. Фінальні бізнес-рішення власника (2026-06-23)

Після аналізу 1080 реальних діалогів з Instagram власник дав остаточні відповіді на
19 питань. Впроваджено в коді + тестах:

### 10.1 Ціни та розрахунок
* **Знижка 10% (лояльність):** бот НЕ пропонує і НЕ рахує — лише людина. Єдина знижка
  бота — УБД.
* **УБД −20%:** на ВСЕ бронювання родини (всі номери), одразу в розрахунку
  (`finalize_quote` → `build_quote_reply(ubd_booking=True)`, знижка на загальний total).
  Текст: "Потрібно надати хоча б копію посвідчення УБД при поселенні" (`MILITARY`).
* **Соло-гість:** завжди `одномісне_поселення` з `pricing.json` (де є; для Напівлюкса null
  → базова ціна).
* **Діти:** 0–5.99 безкоштовно (без окремого місця); 6–11.99 → `дитяче_місце`; 12+ →
  `додаткове_місце`. Базова місткість 2 для УСІХ типів (`pricing_engine`).
* **Великі групи 20+:** ЗАВЖДИ перенаправлення до Ярослава (`LARGE_GROUPS_EVENTS`).
* **Міжсезоння:** НЕ відмовляємо — `OFF_SEASON` ("Наразі вартість на ці дати ще
  узгоджується.") + мітка `Instagram` для менеджера (`bot_server`), без прохання телефону.

### 10.2 Розселення (split)
* **6+ гостей в одному номері:** спершу запит `ASK_ROOM_DISTRIBUTION` ("…як вас краще
  розселити: скільки пар, хто з дітьми?") — `dialogue_engine.plan` (один room-об'єкт із
  ≥6 гостей). Явне мульти-бронювання (>1 номер) — проходить далі.
* **Родина 4 (батьки + 2 дітей):** пріоритет 1) Напівлюкс → 2) два окремі номери (зручніше)
  → 3) один номер з диваном. Лише 3 публічні назви. `finalize_quote_all` додає рекомендацію.

### 10.3 Зручності та логістика (шаблони)
* **Харчування:** `FOOD_PRICES` без змін; колиба на території, відкрита щодня для всіх.
* **Холодильник:** у Стандарт+ та Напівлюкс; у Стандарт — НЕМАЄ (`PRESENTATION_ROOMS`).
* **Діти:** немає ліжечок/манежів; Є дитячий басейн; усі включені зручності доступні
  (`CHILDREN_AMENITIES`).
* **Чани/сауна:** на стадії будівництва до грудня; партнери за 2 хв; деталі при поселенні.
  **Тенісного корту НЕМАЄ** — ніколи не згадуємо (`SAUNA_VATS` + правило в екстракторі).
* **Трансфер/паркінг:** `TRANSFER_PARKING` (паркінг у вартості; трансфер ~1000 грн).
* **Заїзд/виїзд:** 14:00 / 12:00; ранній/пізній — безкоштовно, якщо номер вільний
  (`CHECK_IN_OUT`).
* **Тварини:** 300 грн/добу; котики на тих самих умовах, що й собаки (`PETS`).

### 10.4 Політики та edge-cases
* **Скасування:** безкоштовно за 10 днів до заїзду (`BOOK_ROOM`).
* **Аванс:** завжди 50%, 72 години на оплату, без холдів/листів очікування (`BOOK_ROOM`).
* **Документи:** рахунок/акт/фіскальний чек за запитом; копії свідоцтв про народження
  дітей; обмін Booking.com → менеджер (`DOCUMENTS`).
* **Блогери/бартер:** новий намір `barter` (+ детермінований `bot_logic.is_barter` ПЕРЕД
  спамом). Бот МОВЧИТЬ, але додає мітку `Instagram` для оператора (`bot_server`).

## 11. Стійкість і детерміновані запобіжники (ПОВНИЙ перелік — restore anchor)

Цей розділ — вичерпний знімок усіх детермінованих механізмів, щоб після `/clear` читання
цього файлу + `CLAUDE.md` + `skills.md` відновлювало 100% контексту.

### 11.1 Порядок обробки в `process_incoming_message` → `_handle_incoming`
`mute → barter(детект) → spam → payment → phone → LLM-екстракція → злиття слот-пам'яті →
no-dates-режим → велика група / faq_override → bare-confirmation → barter(topic) → unknown →
маршрутизація (route_simple_topic інакше plan)`. Перші чотири (mute/barter/spam/phone) —
короткі замикання БЕЗ LLM.
* **mute** (`is_muted`, мітка `Замовлено`) → повна тиша (людина веде діалог).
* **barter** (`is_barter` маркери: бартер/блогер/рілс/reels/взаємопіар/колаборац/огляд за
  проживання) → тиша + мітка `Instagram` (готель ХОЧЕ колаборацію, опрацьовує людина).
* **spam** (`is_spam`, B2B-маркери) → повна тиша, без мітки.
* **payment** (`is_payment_intent`: вкладення-скрін АБО keyword оплати) →
  `PAYMENT_RECEIVED_HANDOFF` + мітка `Замовлено` + тиша (НІКОЛИ не авто-підтверджує).
* **phone** (`contains_phone_number`, 9–13 цифр) → `PHONE_RECEIVED`, передача менеджеру.

### 11.2 Стійкість до збоїв LLM (Gemini 503/429) — graceful degradation
* `generate_with_retry(prompt, retries=3, base_delay=2)`: екс-бекоф **2s, потім 4s**, лише
  на ТРАНЗИТНИХ помилках (`_is_retryable_llm_error`: 503/429/500/unavailable/
  resource_exhausted/overloaded/high demand/timeout/deadline/internal error). Нетранзитні
  (напр. 400) — підіймаються одразу.
* Якщо екстракція все одно падає → НЕ мовчимо: надсилаємо `ERROR_LLM_DOWN`
  ("…система зараз трохи перевантажена… менеджер підключиться…") ОДИН раз (не повторюємо,
  якщо останнє повідомлення бота вже було цим текстом) + мітка `Instagram`. Захищено
  перевіркою `_superseded`.
* Жива симуляція підтверджує: транзитний 503 → холдинг-меседж, потім повторний тригер
  дає коректний результат (квоту/відповідь).

### 11.3 Асинхронність / дедуп / пам'ять (per-conversation, у `bot_server`)
* **Лок per-conv** (`_lock_for`) серіалізує обробку одного діалогу.
* **Supersede** (`_conv_seq`/`_superseded`): drip-burst із кількох повідомлень → ОДНА
  консолідована відповідь; перерваний хід глушиться, але скрейп уже наповнив кеш.
* **Ідемпотентне привітання** (`_greeted`) — одне привітання попри read-after-write lag.
* **Слот-пам'ять** (`_slot_memory[conv]` = СПИСОК кімнат): `merge_rooms`/`remember_rooms`
  дозаповнюють порожні поля з пам'яті по індексу, зберігають незгадані кімнати; нові
  непорожні значення виграють; дати успадковуються лише коли свіжий хід їх не згадав.
* **`_pending_window`** — (checkin,checkout) запропонованого вікна для контекстного «Так».
* **`_no_dates_mode`** — клієнт сказав «дати ще не знаю» → наступний хід із гостями робить
  проактивний скан, а не цикл питання про дати.
* **`AVAILABILITY_CACHE`** (TTL 900с): `get_hotel_data_cached` (скрейп + "Секундочку"),
  `peek_cached_availability` (БЕЗ скрейпу — для FAQ+кеш-комбо).

### 11.4 Класифікація басейну (GUEST_POOL vs POOL) — детерміновано
`bot_logic._pool_template`: маркери купання/відвідування без проживання або ціни басейну
(окрім «входить у вартість») → **GUEST_POOL** (ціни для невідвідувачів); загальне питання
про басейн як зручність → **POOL**. `faq_override` ПРИМУСОВО застосовує цей розподіл для
будь-якого «басейн» (перекриває екстрактор). Дзеркалиться правилом у `EXTRACTION_PROMPT`.

### 11.5 Розподіл по номерах та межі
* **6+ гостей в одному room-об'єкті** → `ASK_ROOM_DISTRIBUTION` ПЕРЕД будь-якою квотою
  (`dialogue_engine.plan`; макс ~5 в Напівлюксі). Явне мульти-бронювання (>1 номер) — далі.
* **Родина 4–5 з дітьми** → `finalize_quote_all` рекомендує Напівлюкс + два окремі номери.
* **Велика група 20+** (`looks_like_large_group` за текстом АБО `slots_total_guests` ≥
  `LARGE_GROUP_MIN=20`) / захід → `LARGE_GROUPS_EVENTS` (Ярослав 0673445220).
* **Мульти-бронювання** до 8 номерів (`MAX_ROOMS_PER_BOOKING`); ідентичні кімнати
  згортаються в один рядок «за N номери типу X».

### 11.6 Ціни/вік (owner 2026-06-23) — стисло (деталі §5, §10, skills.md §1)
* Діти: **0–5 безкоштовно; 6–11 `дитяче_місце`; 12+ `додаткове_місце`** (напіввідкриті межі:
  6 → дитяче, 12 → додаткове). Базова місткість **2 для УСІХ типів**.
* Соло → завжди `одномісне_поселення`. УБД **−20% на ВСЮ суму бронювання** (всі номери).
* Ціни лише Червень/Липень/Серпень; інше → `OFF_SEASON` ("ще узгоджується") + мітка
  `Instagram`. Знижку 10%/лояльність бот НЕ рахує і НЕ обіцяє.
* Ночі = виїзд − заїзд (день виїзду не оплачується); вихідні = Пт і Сб (per night).
* Blacklist категорій (`Колиба/Басейн/Overbooking`) + згортання внутрішніх назв у 3
  публічні типи (`build_simplified_availability`).

### 11.7 Уточнення власника (2026-06-24)
* **Дати зберігаються на 6+ розбивці.** Коли клієнт розбиває одне бронювання на кілька
  номерів і НЕ називає нових дат, кожен номер успадковує дати запам'ятованого перебування
  (`bot_logic.merge_rooms` + `_shared_booking_dates`) — рушій СКАНУЄ з цими датами, а не
  перепитує. Дати, названі цього ходу, мають пріоритет; вже наявні дати номера не
  перезаписуються. (Жива перевірка: «нас 7» → `ASK_ROOM_DISTRIBUTION`; «2 номери 4 і 3» →
  скан на тих самих датах без повторного питання.)
* **УБД-нота на sold-out вікнах.** Якщо `ubd=true`, але дати зайняті, до `ROOM_BOOKED` та
  `SOLD_OUT_FOUND_NEAREST` (у `finalize_quote` і `finalize_quote_all`) додається
  `templates.MILITARY` (`dialogue_engine._with_ubd_note`) — щоб ветеран знав, що −20%
  діятиме й на запропоноване найближче вікно.
* **Тестові персони (`simulate_live_chat.py`).** Стокові персони — реалістичні (без назв
  типів номерів у першому повідомленні). REGRESSION-набір лишає лише УНІКАЛЬНІ сценарії
  (проактивний скан без дат; 6+ розбивка — Fix; УБД на sold-out — Fix); дублі соло/діти-6/
  діти-12/УБД-2-номери прибрано (їх покривають юніт-тести + стокові персони). Затримки
  конфігуруються через env (`SIM_DRIP`/`SIM_PERSONA`/`SIM_SETTLE`).

### 11.8 Жорстка місткість номерів + UX знижки (2026-06-24)
* **Жорстка місткість (`pricing_engine.fits_room`).** Стандарт / Стандарт+ (і підтипи):
  МАКС 3 дорослих; МАКС 4 особи всього; дитина 12+ рахується як дорослий (3 дорослих +
  дитина 12+ = понад місткість). Напівлюкс: до 5 осіб. `quote()` кидає `OverCapacityError`,
  `price()` повертає `None`. Над-місткісні типи ВІДФІЛЬТРОВУЮТЬСЯ з пропозицій
  (`finalize_quote_all` пропускає; обраний замалий номер у `finalize_quote` →
  перенаправлення на типи, що ВМІЩУЮТЬ). У мульти-бронюванні над-місткісний номер НЕ губить
  інші — збираємо `too_small` і віддаємо `ROOM_TOO_SMALL` (називаємо проблемні номери,
  просимо перерозподілити). Пошук найближчого вікна (`nearest_window_any`) теж зважає на
  місткість — ніколи не пропонує вікно, де вільний лише замалий номер.
* **UX знижки на дитяче місце.** Якщо в котируванні є дитина 6–11 (`дитяче_місце`),
  `dialogue_engine.quote_line` додає до рядка ` (враховано знижку 50% на дитяче місце)`.

### 11.9 Класифікація оплати + drip-таймінг (2026-06-24)
* **Оплата по приїзду → BOOK_ROOM (детерміновано).** Питання про ПРАВИЛА оплати
  ("по приїзду оплатити", "без передоплати", "яка передоплата", "коли платити") тепер
  пінятся детерміновано: `bot_logic.is_payment_rules_question` у `faq_override` → `BOOK_ROOM`.
  Промпт-правило в `EXTRACTION_PROMPT` (приїзд+оплата = передоплата, НЕ CHECK_IN_OUT) лишилось,
  АЛЕ виявилось ненадійним у контексті бронювання (LLM плутав із booking-топіком), тому
  додано детермінований запобіжник. "приїзд" САМ ПО СОБІ (без оплати) лишається CHECK_IN_OUT;
  завершена оплата ("оплатив"/"квитанція") ловиться раніше через `is_payment_intent`.
* **Drip-таймінг симулятора.** `simulate_live_chat.py` default `DRIP_DELAY` піднято до
  `(20, 23)` с — на швидких дрипах Chatwoot змішував FAQ-відповіді й бот пропускав
  фінальні питання. (Файл gitignored — лише локально.)

### 11.10 Жива QA 2 — крайові випадки, галюцинації, UX (2026-06-24)
1. **Презентація перед розбивкою.** `ASK_ROOM_DISTRIBUTION` і `ROOM_TOO_SMALL` тепер
   ПЕРЕДУЮТЬ `PRESENTATION_ROOMS` (через `[SPLIT]`) — клієнт спершу бачить місткості
   (Стандарт/Стандарт+ = макс. 3 дорослих), потім питання як розселитись.
2. **Без «галюцинацій» опису номерів.** «Параграф про 11 номерів, Хомʼяк, Боярин» був
   шаблоном `GENERAL_INFORMATION` (а не галюцинацією LLM). Шаблон переписано на чистий
   опис 3 публічних типів без внутрішніх назв; `EXTRACTION_PROMPT` посилено: екстрактор
   ЛИШЕ повертає JSON і не вигадує описи/назви кімнат.
3. **Немає даних = SOLD OUT.** `bot_logic.is_room_available`: дата, ВІДСУТНЯ у відсканованому
   календарі номера, тепер за замовчуванням `sold_out` (а не `unknown`) — ніколи не
   котируємо дату, яку OtelMS не підтвердив вільною. Якщо ж відсутня вся категорія
   (scraper-фейл) — лишається `unknown` (поблажливо). (Скасовує стару поведінку «поза вікном = unknown».)
4. **Анти-дублі + тваринка.** `_emit` глушить повідомлення, що збігається з ОДНИМ ІЗ
   ОСТАННІХ 2 повідомлень бота (дрип повторював FAQ 3×). Якщо клієнт згадав тваринку
   (`bot_logic.mentions_pet`) — до котирування додаємо «(вартість вказана без урахування
   доплати за тваринку 300 грн/доба)».
5. **Ніколи не питаємо дозволу на дати.** `NEAREST_NONE` переписано (без «можливо вас
   зацікавлять інші дати?»); `finalize_quote` перед `NEAREST_NONE` пробує
   `nearest_window_any` (будь-який номер, що ВМІЩУЄ гостей) — завжди пропонуємо вікно.
6. **Нові FAQ.** `HAIRDRYER` (фен у кожному номері), `MEDIA` (фото/відео у сторіс/стрічці);
   `faq_override` маршрутизує «фен»→HAIRDRYER, «фото/відео»→MEDIA; `is_location_question`
   посилено; виправлено друкарську помилку «Темпаратура»→«Температура» в `POOL`.
7. **Кулдаун після 503.** `bot_server._cooldowns[conv]`: після `ERROR_LLM_DOWN` бот ІГНОРУЄ
   всі повідомлення цієї конверсації 5 хв (`COOLDOWN_TTL=300`), щоб людина перехопила
   діалог без повторних збоїв.
8. **Останнє слово.** Гола подяка/прощання (`bot_logic.is_pure_thanks`, до 4 слів, без
   цифр/підтверджень/предметних слів) → `ACKNOWLEDGE_THANKS` (детерміновано, без LLM) —
   бот ніколи не мовчить на «Дякую».
